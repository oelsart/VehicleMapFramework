using Verse;

namespace VehicleMapFramework;

public class CompProperties_ZiplineBelt : CompProperties
{
    public CompProperties_ZiplineBelt()
    {
        compClass = typeof(CompZiplineBelt);
    }
}